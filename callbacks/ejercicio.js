/*function estudiar( callback=()=>{}){
    let  terminarDeEstudiar=false
    console.log('estoy estudiando')
    terminarDeEstudiar=false
    if(terminarDeEstudiar)callback(null,'listo termine de estudiar')
    if(!terminarDeEstudiar)callback('silencio','sigo estudiando:v')
    }
    estudiar((error,mensaje)=>{
        if(error) console.error(error, mensaje)
        console.log(mensaje)
    })*/

    //Funcion callBack
    //setTimeout(()=>console.log('Esto se va ejecutar despues de 3 segundos'), 3000);